    malloc() : Alloue dynamiquement un bloc de mémoire spécifié par sa taille en octets.

    calloc() : Alloue dynamiquement un bloc de mémoire spécifié par sa taille en octets et l'initialise à zéro.

    realloc() : Modifie la taille d'un bloc de mémoire alloué précédemment.

    free() : Libère un bloc de mémoire alloué dynamiquement précédemment à l'aide de malloc(), calloc() ou realloc().

    exit() : Termine immédiatement le programme et renvoie un code de sortie spécifié.

    atoi() : Convertit une chaîne de caractères en un entier.

    atof() : Convertit une chaîne de caractères en un nombre à virgule flottante.

    rand() : Génère un nombre pseudo-aléatoire.

    srand() : Initialise le générateur de nombres pseudo-aléatoires avec une graine.

    abs() : Calcule la valeur absolue d'un entier.

    labs() : Calcule la valeur absolue d'un long entier.

    strtod() : Convertit une chaîne de caractères en un nombre à virgule flottante double précision.

    strtol() : Convertit une chaîne de caractères en un long entier.

    strtoul() : Convertit une chaîne de caractères en un long entier non signé.
