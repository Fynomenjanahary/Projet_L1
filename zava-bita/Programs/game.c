#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(){
	srand(time(NULL));
/**
	printf("\nRAND_MAX = %d \ttime = %ld\n",RAND_MAX, time(NULL));
/// Générer 10 nombres pseudo-aleatoire < RAND_MAX
	for(int i=0 ; i<10 ; i++)
		printf("%d\t", rand());
	printf("\n");
		
/// Générer des nombres entre 1 et 100: cf. Numerical Recipes
	for(int i=0 ; i<10 ; i++)
		printf("%d\t", 1+ (int)(100.0*rand() / (RAND_MAX +1.0)));
		

for(int i=0 ; i<10 ; i++)
		printf("%d\t",100*rand() / (RAND_MAX ));
**/
	int nbr;
	nbr = 1+ (int)(100.0*rand() / (RAND_MAX);
	int dev=0;
	while(dev!=nbr){
		
		
		
	}	
	return 0;
}
